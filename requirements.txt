# Runtime dependencies for pctrans (pre-clinical to clinical translation).
#
# These mirror the top-level [project.dependencies] in pyproject.toml, pinned to
# the versions this project was developed and evaluated against. Transitive
# dependencies are resolved by pip on the target platform, so this file installs
# cleanly on Linux (Streamlit Community Cloud), macOS, and Windows alike.
#
# For the exact locked environment (dev tools included) use `uv sync` with uv.lock.
# The Streamlit app imports pctrans.data.dataset, which pulls in torch — it is a
# required runtime dependency even though the app itself never loads the model.

torch==2.12.1
pandas==2.3.3
numpy==2.4.6
pyarrow==24.0.0
scikit-learn==1.9.0
umap-learn==0.5.12
numba==0.65.1
llvmlite==0.47.0
streamlit==1.58.0
plotly==6.8.0
matplotlib==3.11.0
mlflow==3.14.0
typer==0.26.8
pyyaml==6.0.3
requests==2.34.2
tqdm==4.68.3
